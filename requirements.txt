"Display each chart in its own separate, full-width Plotly chart. Do not combine multiple charts into one figure using subplots. Each chart should have its own title, axis labels, and height. Use st.plotly_chart(fig, use_container_width=True) for every chart. Organize the charts using st.tabs() so the user can switch between them cleanly without scrolling."streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28
plotly>=5.18.0
xgboost>=2.0.0
scikit-learn>=1.3.0
ta>=0.10.2
joblib>=1.3.0
pyarrow>=14.0.0
